# Studio Widget

Phase 9 Studio is the page-level workspace that binds project detail query, timeline save mutation, project metadata, and the `TimelineEditor` feature.

It must not call backend endpoints outside the typed API client.
