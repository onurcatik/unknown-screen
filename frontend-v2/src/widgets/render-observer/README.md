# Render Observer Widget Boundary

Phase 10 keeps the actual observer in Studio through `features/render-project` because the backend requires a project id and job id. A global render observer page is intentionally not implemented until the backend exposes a list-jobs endpoint.
